#ifndef _MAIN_
#define _MAIN_
#include <string>

//Declare variables that are going to be used by all the classes
namespace global{
    extern string key;
    extern string id;
    extern string host;
}

#endif
